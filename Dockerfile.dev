FROM eclipse-temurin:17-jdk

# tiny tools for health/debug
RUN apt-get update && apt-get install -y --no-install-recommends curl && \
    rm -rf /var/lib/apt/lists/*

WORKDIR /workspace
# code will be mounted; just start bootRun
CMD ["bash","-lc","./gradlew --no-daemon bootRun"]
